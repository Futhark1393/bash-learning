'#!/bin/bash'

# Variable Definition (NO SPACE after the equals sign)
name="Futhark"
os="Fedora Linux"
version=43

# Using the Variable (called with the $ sign)
echo "Hello, I'm $name."
echo "My OS is: $os version $version"
